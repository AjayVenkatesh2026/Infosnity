<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.prasadthegreat.infosnity">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Infosnity">
        <activity android:name=".chatActivity"></activity>
        <activity android:name=".faculty.createclassActivity" />
        <activity android:name=".faculty.MainActivityfaculty" />
        <activity android:name=".roleActivity" />
        <activity android:name=".signinActivity" />
        <activity android:name=".startingActivity" />
        <activity android:name=".MainActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>